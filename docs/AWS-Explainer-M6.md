# AWS Explainer — M6

This file is the renamed M6 AWS deployment/rebuild reference previously stored as `AWSexplainer.md`. The original content is preserved under the new filename.

> Runtime secrets are intentionally omitted.

For the complete M6 deployment history, see the project progress and architecture documentation.
