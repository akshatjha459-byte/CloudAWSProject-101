# CloudAWSProject-101

AWS Cloud Design and Architecture project.

## Structure

- `modules/` — 10 project modules
- `infrastructure/` — AWS infrastructure and deployment definitions
- `scripts/` — deployment and utility scripts
- `docker/` — Docker configuration
- `database/` — database schema and seed data
